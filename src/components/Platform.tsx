import { styled } from '../styles/theme'

export const Foundation = styled('div', {
  background: '$white',
  color: '$black920',
  minHeight: '100vh',
  transition: 'all .15s ease-in-out, background 0'
})

export const Context = styled('div', {}) // NOTE: left for future work
